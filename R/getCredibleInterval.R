#getCredibleInterval

#' @export
getCredibleInterval<-function(df){
  #compute 95% credible interval (bayesian approach)
  #
  #Args:
  #dataframe:first column is name(string), second column is score(numeric)
  #
  #Return:
  #dataframe
  #
  return(NULL)
  
}